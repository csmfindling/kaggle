creates both the training and submit datasets. The first script to launch is the get_data that loads the images and creates a pickle file. The second script build_hdf5_dataset creates hdf5 datasets.

Both both will look for the data in '../../data'. The output will also be put in '../../data'
